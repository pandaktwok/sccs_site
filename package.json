{
  "name": "sccs-backend",
  "version": "1.0.0",
  "description": "Backend API for SCCS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "test:all": "npm test && cd frontend && npm test",
    "build:frontend": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-fileupload": "^1.5.2",
    "fs-extra": "^11.3.4",
    "rss-parser": "^3.13.0",
    "webdav": "^5.9.0"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "nodemon": "^3.1.0",
    "supertest": "^7.2.2"
  }
}
